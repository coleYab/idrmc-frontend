import ActivityLogClient from './activity-log-client';

export const metadata = {
  title: 'Activity Log - Admin Console'
};

export default function AdminActivityPage() {
  return <ActivityLogClient />;
}
